# Progress log

## Session 1
- Empty repo confirmed. Plan "Omnlib" approved (user added Phase 7 import later).
- Decisions: single-user-first/multi-user-ready schema, local-first SQLite, Next.js+Drizzle, books via Google Books+OpenLibrary+AniList NOVEL.
- Goal goal-9de2ab81 (24 rounds), todo list, planning files created.
- Sandbox saga: pnpm unusable (read-only store), npm works; scaffolded Next 16.3.4 via create-next-app into subdir `oms`, moved to root, renamed package.
- Planning files lost via transient /tmp → regenerated (this file).
- TODO: install drizzle deps (bg job), read Next 16 docs, write schema + migrate, boot dev server.
- Phase 1 COMPLETE: providers (anilist/tmdb/googlebooks/openlibrary), normalize.ts, ratelimit.ts, fixture tests 9/9 Jive: AniList search/byId+43 relations, Open Library live OK; Google Books 429 without key (documented); TMDB needs key.
- Found+fixed toHttps bug via tests (http->https).
- Phase 2 START: ingest core.
- Phase 2 COMPLETE: ingestRecord (idempotent upsert + external_ids + episodes + relations staging resolver) + CLI. Acceptance on fresh accept.db: ingest anilist:21 x2 → media=1 stable; ingest 30013 → bidirectional adaptation links resolve (relation graph works).
- Phase 3 START: API routes (search, media, library, progress, timeline, stats).
- Phase 3 COMPLETE: 8 API routes verified live (search federated, add-to-catalog, library CRUD, progress bump, timeline, stats, schedule).
- Phase 4 COMPLETE: "Cosmic Shelf" UI (home search + continue shelf, media detail + story-universe relation graph, library shelves, timeline, stats, schedule page), PWA manifest+SW+icon. All pages 200; production build green (14 routes).
- Phase 5 COMPLETE: schedule API/page (One Piece ep 1177 in next-14-days), sync job run OK (5 items refreshed).
- Phase 6 COMPLETE: README, .env.example, timeouts/fail-fast hardening, FTS fix, AniList NOVEL->MANGA+format fix (live quirk).
- Final: tsc clean, vitest 9/9, build 14 routes, acceptance: 4 formats tracked (anime manga light_novel book), cross-format adaptation graph live (Mushoku Tensei LN<->anime), stats/timeline/schedule working. Webseries live needs TMDB_API_KEY (documented).
- FEATURE (user request): in-app live sync — no terminal needed. lib/etl/refresh.ts shared engine; POST/GET /api/sync; SyncButton in Nav + Library header; per-item Refresh + once-per-session auto-refresh on media pages for releasing titles. Tested live (updated ONE PIECE x2, single item sync OK, all pages 200); build green (15 routes incl /api/sync).
- FEATURE (user request): one-command startup. scripts/omshelf launcher (self-locating, auto-migrate on first run, dev/start/migrate/status/help, OMS_PORT) + scripts/install-omshelf.sh installs `omshelf` into ~/.local/bin. Installed live; verified running from /tmp cwd; old bg dev server replaced by launcher-owned server.
- FEATURE: omshelf stop command added (lsof/fuser/pgrep fallback). Instances stopped at user request; port 3000 closed; README updated.
- FEATURE (user answered: movies + franchise collections): added movies as 6th format (AniList ANIME+format MOVIE keyless; TMDB movie endpoints keyed) + Collections (TMDB belongs_to_collection): schema media_type movie + media.runtime_minutes + collections + media_collections (migration 0002, additive, no table rebuild). Ingest runtime+collection upsert/link; API /api/collections (list + members); UI: 6th accent color (gold), Home/Library movie chips+groups, media page movie 100%-style watch progress + "Part of" collection card, Collections pages; movies route total=100. Tests 12/12. Live: launched server, ingested Kimi no Na wa as movie, watched -> progress 100 completed. Build green 18 routes. Live-action film search/collections need TMDB_API_KEY.
- LIVE TMDB (user provided key): verified api_key style 200; movies/webseries/collections now fully live. Found+fixed real bug: TMDB movie/TV share one id namespace -> added byMediaType(providerId, hint) + mediaType hint in POST /api/media (home page passes hit.mediaType). Interstellar saved as movie (169 min, watched -> 100%), Iron Man -> 'Iron Man Collection' created+linked, /api/collections lists it. Pruned stray TV episodes from the earlier wrong-type row. .env.local gitignored.
- Phase 7 COMPLETE: import engine (lib/import: parsers for AniList JSON/MAL XML with fast-xml-parser/IMDb CSV/Goodreads CSV; resolve.ts with TMDB findByImdb + AniList MAL match + ISBN lookup + fuzzy; run.ts gaps-only idempotent merge with importedFrom/Ref provenance + imported activity). /api/import + /import UI page + pnpm import-file CLI. Fixed real quirks: IMDb export header lowercase (title/titleType), fast-xml-parser single-node shape, first-provenance-wins dedupe. Live verified: anilist 2/2 matched idempotent, imdb 2/2 (Interstellar movie + Game of Thrones webseries via TMDB find). Tests 19/19, build green.
- UI PASS (no features touched): reintroduced + documented an OniList style design system v2 in globals.css (tokens, .h1-display/.eyebrow/.badge/.seg/.icon-btn/.navlink/.skeleton/.card-hover/.cover-veil, focus-visible, prefers-reduced-motion, shimmer/spin). Nav: link-pills with animated active underline + aria-current + mobile wrap + footer. Home: editorial hero + cover-led 5-col search grid with veil hover + progress bars. Media page: banner hero overlay, sticky cover, segmented status/rating controls, +/- icon buttons, relation graph as cards, loading skeleton. Library: skeletons, cover-veil, count badges, tightened header. Stats/timeline/schedule/collections/import: header + card polish. All handlers/fetches unchanged. Verified: tsc clean, tests 19/19, build green, all 8 pages 200, search/library/stats/sync APIs working, new CSS tokens served. Screenshots at /tmp/shots/.
- FEATURES: (1) Trending on Home pre-search — /api/trending (AniList TRENDING anime/manga keyless + TMDB weekly tv/movie), sections render only when query empty, disappear while searching; HitCard/HitGrid extracted & shared with results. (2) Stats overhaul — /api/stats now returns zero-filled 12-month monthlyActivity + completionRate + genreCount; page rebuilt with SVG area/line chart, genre donut with legend + center stat, status breakdown bars, 8 hero metrics. Verified in real browser DOM (trending titles render; charts sections render). Screenshots /tmp/shots/{trending,stats}.png. Note: user-reported "one issue in next console" not reproducible headlessly — requested exact message.
- RENAME: app rebranded OmniShelf -> Omnlib (layout metadata/nav/footer, manifest, SW cache omnlib-v1, user-agent Omnlib/0.1, import copy, package.json name omnlib). CLI renamed omshelf -> omnlib (scripts/omnlib + install-omnlib.sh; ~/.local/bin/omnlib installed; old omshelf symlink removed; verified command resolves). GitHub README rewritten (features, install, CLI guide, web guide, import, structure, dev, roadmap, limitations, license) + MIT LICENSE.
- FIX: Nav hydration mismatch (usePathname in static shared layout) — active state now post-mount only; verified 0 hydration errors on / and /stats in headless Chrome.
- Verified: tsc clean, tests 19/19, build green, server runs via omnlib, brand clean everywhere (CSS comment fixed).
- FEATURE (user request): Terminal CLI — scripts/omnlib-cli.ts talks to the RUNNING server over the same /api/* routes the dashboard uses (zero logic duplication, single engine). Commands: list (filters + --json), find (search, prints refs), add (search-and-add with --index/--yes picker, or provider refs anilist:21/tmdb:550/googlebooks:ISBN/openlibrary:/works/…), show, set (status/rating/notes), progress +N/-N/=N (both routed through the progress endpoint so auto-complete stays in one place), rm (confirm unless --yes), sync --limit, import <source> <file>. Exit codes 0/1/2; --json on every data command (machine/AI ready — the foundation for the deferred MCP server). Launcher scripts/omnlib now routes these subcommands, FIXED the bug where `omnlib sync` fell through to dev, unknown commands error instead of silently starting dev. tests/cli.test.ts 19 unit tests (total 38). Live-verified every command against the running server (search/add/dedupe/show/rm/progress/set/sync/import; Google Books 429 surfaced properly — needs API key as documented). Test artifacts cleaned up (lib restored to 8 entries). README + help updated. tsc clean, 38/38 tests, build green (24 routes).
